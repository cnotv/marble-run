// vite.config.js
module.exports = {
  root: './public', // the root directory for your project
  build: {
    outDir: '../dist', // the output directory for the build
  },
  server: {
    port: 3000, // the port to run your dev server on
  },
};